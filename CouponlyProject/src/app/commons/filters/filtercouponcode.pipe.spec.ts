import { FiltercouponcodePipe } from './filtercouponcode.pipe';

describe('FiltercouponcodePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltercouponcodePipe();
    expect(pipe).toBeTruthy();
  });
});
