import { FiltercouponnamePipe } from './filtercouponname.pipe';

describe('FiltercouponnamePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltercouponnamePipe();
    expect(pipe).toBeTruthy();
  });
});
