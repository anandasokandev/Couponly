import { FilteremailPipe } from './filterstore.pipe';

describe('FilteremailPipe', () => {
  it('create an instance', () => {
    const pipe = new FilteremailPipe();
    expect(pipe).toBeTruthy();
  });
});
